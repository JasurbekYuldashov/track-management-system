
#database
#spring.datasource.url=jdbc:postgresql://185.8.212.116:9999/tms_db
#spring.datasource.username=velocifero
#spring.datasource.password=1052#dorian

spring.datasource.url=jdbc:postgresql://localhost:5432/lbs
spring.datasource.username=postgres
spring.datasource.password=cuba
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.enable_lazy_load_no_trans=true

#custom banner
spring.banner.location=banner-dev.txt

#server
server.port=8086

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true

# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB

# Max file size.
spring.servlet.multipart.max-file-size=100MB

# Max Request Size
spring.servlet.multipart.max-request-size=105MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.upload-dir=/uploads